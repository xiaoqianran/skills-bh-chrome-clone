# Changelog

## 0.1.0

### Features

- Agent Skill `bh-chrome-clone` with session-twin policy
- Bundled CLI `bh-clone` (`init` / `sync` / `ensure` / `use` / `doctor`)
- Installer links CLI + skill into `~/.local/bin` and agent skill dirs
- Design + verification notes from local retest (bilibili isLogin + search)

### Docs

- README explains Skill vs MCP and security constraints
